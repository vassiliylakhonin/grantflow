# Kazakhstan NGO R2 Outcome Memo

Date:
- `2026-03-22`

Checkpoint decision:
- `continue`

Case decisions:
- `CASE-UN-01` -> `continue`
- `CASE-EU-01` -> `continue`
- `CASE-USAID-01` -> `continue_with_conditions`

## Executive Read

The pilot remains directionally successful after the first full `R2` checkpoint.

What is now clear:
- `UN agencies` and `EU` have moved through `R2` without extra intervention and show stable improvement versus baseline
- `USAID` improved after corrective actions, but still carries higher coordination and review noise than the other two tracks
- the weak spot remains localized in the hardest donor workflow rather than spread across the pilot

## R2 Results Versus Baseline

| Case ID | Donor | Baseline first reviewable draft | Actual first reviewable draft | Baseline final package | Actual final package | Review loops baseline -> actual | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `CASE-UN-01` | `un_agencies` | `46h` | `32h` | `132h` | `109h` | `4 -> 3` | `continue` |
| `CASE-EU-01` | `eu` | `58h` | `42h` | `176h` | `149h` | `5 -> 4` | `continue` |
| `CASE-USAID-01` | `usaid` | `64h` | `52h` | `214h` | `186h` | `6 -> 5` | `continue_with_conditions` |

## Outcome By Donor Path

### UN agencies

- first reviewable draft improved from `46h` to `32h` (`-30.4%`)
- final package improved from `132h` to `109h` (`-17.4%`)
- review loops reduced from `4` to `3`
- duplicate/conflicting comments reduced to `3`
- major MEL rewrites reduced from `2` to `1`

Read:
- pilot value is visible and operationally stable
- continue without additional intervention

### EU

- first reviewable draft improved from `58h` to `42h` (`-27.6%`)
- final package improved from `176h` to `149h` (`-15.3%`)
- review loops reduced from `5` to `4`
- duplicate/conflicting comments reduced to `6`
- major MEL rewrites reduced from `3` to `1`

Read:
- first-draft speed target is met
- review process is improving, though still noisier than `UN`
- continue without workflow redesign

### USAID

- first reviewable draft improved from `64h` to `52h` (`-18.8%`)
- final package improved from `214h` to `186h` (`-13.1%`)
- review loops reduced from `6` to `5`
- duplicate/conflicting comments reduced from the `R1` spike to `8`, but remain highest across the pilot
- major MEL rewrites improved to `2`, but the path still shows heavier late-stage alignment than `UN` and `EU`

Read:
- corrective actions worked, but not fully
- USAID should continue, but under conditions
- next gains are likely to come from tighter coordination discipline, not core product redesign

## Pilot-Level Interpretation

The pilot should continue.

Reasoning:
- all three cases completed `R2`
- all three improved versus baseline on first draft, final package cycle, and review loops
- two of the three donor paths are now performing in the target range for first-draft acceleration
- the remaining weak path is the most coordination-heavy donor workflow and is improving in the expected direction

## Recommended Operating Read

- `UN`: continue normally
- `EU`: continue normally
- `USAID`: continue with conditions

## Conditions For USAID Going Forward

1. keep the evidence package frozen before broader review expansion
2. preserve a single consolidated finance/compliance pass
3. hold MEL alignment before wider reviewer circulation
4. enforce comment cutoff discipline and reviewer routing through the operator

## External Read

This checkpoint is strong enough to support an external pilot update with a clear caveat:
- the pilot is producing measurable operational gains
- the strongest proof points are `UN` and `EU`
- `USAID` is improving, but still needs tighter review discipline to reach the same operating level
