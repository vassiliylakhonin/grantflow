# Настройка Qwen OAuth для OpenClaw

## Дата настройки
2026-02-20

## Предыстория
Использовали бесплатный тариф Qwen через OAuth как fallback для Codex, который исчерпал месячный лимит.

## Шаги настройки

### 1. Проверка доступных моделей
```bash
openclaw models list
```

### 2. Настройка Qwen как модели по умолчанию
```bash
openclaw model set qwen-portal/coder-model
```

### 3. OAuth аутентификация
```bash
openclaw auth login qwen-portal
```
- Открывается браузер для авторизации на qwen-portal
- После успеха токен сохраняется локально

### 4. Проверка статуса
```bash
openclaw status
```
Ожидаемый результат:
```
🧠 Model: qwen-portal/coder-model · 🔑 oauth (qwen-portal:default)
```

### 5. (Опционально) Настройка aliases
В конфигурации OpenClaw добавить alias:
```yaml
models:
  aliases:
    qwen: qwen-portal/coder-model
```

## Текущая конфигурация
- **Модель:** qwen-portal/coder-model
- **Аутентификация:** OAuth (qwen-portal:default)
- **Часовой пояс:** Asia/Almaty (UTC+05:00)
- **Workspace:** /Users/vassiliylakhonin/.openclaw/workspace

## Заметки
- Qwen предоставляет бесплатный тариф с достаточными лимитами для ежедневной работы
- При исчерпании лимитов Codex автоматически переключаться НЕ будет — требуется ручная настройка fallback
- Это и есть основной UX-gap: нет встроенного wizard для настройки запасного провайдера
